apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-devops-webapp.fullname" . }}
  labels:
    {{- include "demo-devops-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-devops-webapp.selectorLabels" . | nindent 4 }}
